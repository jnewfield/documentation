k8snodes:
  hosts:
    host1:
      ansible_host: "{{ host1 }}" 
      ansible_user: ubuntu
    host2:
      ansible_host: "{{ host2 }}" 
      ansible_user: ubuntu
    host3:
      ansible_host: "{{ host3 }}" 
      ansible_user: ubuntu
ngxnodes:
  hosts:
    ngx1:
      ansible_host: "{{ ngx1 }}"
      ansible_user: jake
    ngx2:
      ansible_host: "{{ ngx2 }}"
      ansible_user: jake
vmclusternodes:
  hosts:
    host1:
      ansible_host: "{{ host1 }}" 
      ansible_user: "{{ user1 }}"
    host2:
      ansible_host: "{{ host2 }}" 
      ansible_user: "{{ user2 }}"
    host3:
      ansible_host: "{{ host3 }}" 
      ansible_user: "{{ user2 }}" 
