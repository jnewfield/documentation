# ngx hosts from script ~/.testenv/my/createstacks/vm-cluster-nim.sh
testenv:
  hosts:
    host1:
      ansible_host: "{{ host1 }}"
      ansible_user: "{{ user }}"
    host2:
      ansible_host: "{{ host2 }}"
      ansible_user: "{{ user }}"
